# __双指针__

## __1.同向双指针(滑动窗口/尺取法)__
1. 引用场景：寻找满足特定条件的"最短/最长连续子区间"
2. 运行机制：两个指针L，R都从左端点出发，绝不回退
    · R指针主动向右扩张，负责把新的元素吞进窗口，直到窗口内的状态满足要求
    · L指针负责向右搜索，负责把旧的元素吐出窗口，用于满足要求的前提下寻找最优解
3. 伪代码：
   ```c++
int l = 1,r = 1,sum = 0,ans =2e9;
while(r <= n){
    sum += a[r];
    while(sum >= target){
        ans = min(ans , r - l + 1);
        sum -= a[l];
        l++;
    }
    r++;
}
   ```